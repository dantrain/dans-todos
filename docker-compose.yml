version: "3.8"

services:
  app:
    build: .
    ports:
      - 8080:80
  db:
    image: postgres:13-alpine
    restart: always
    environment:
      POSTGRES_PASSWORD: example
    ports:
      - 5432:5432
